# Copy this file to `.env.local` and fill in your real credentials.
# `.env.local` is gitignored — never commit it.

# Admin login (used by the server action in app/(auth)/login/actions.ts)
ADMIN_EMAIL=
ADMIN_PASSWORD=
